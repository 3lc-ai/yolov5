# ty config for the 3LC integration in this fork.
#
# Scoped to utils/loggers/tlc/ so the upstream YOLOv5 code (much of which is
# untyped) is not analyzed. Mirrors the rule set used in 3lc-ultralytics.

[environment]
python-version = "3.10"
root = ["."]

[src]
include = ["utils/loggers/tlc/"]

[rules]
# Start with all rules disabled, then enable selectively.
all = "ignore"

# Errors: rules that catch real bugs.
call-non-callable = "error"
conflicting-declarations = "error"
conflicting-metaclass = "error"
deprecated = "error"
division-by-zero = "error"
duplicate-base = "error"
index-out-of-bounds = "error"
invalid-context-manager = "error"
invalid-exception-caught = "error"
invalid-raise = "error"
invalid-return-type = "error"
invalid-type-form = "error"
missing-argument = "error"
no-matching-overload = "error"
not-iterable = "error"
possibly-missing-attribute = "error"
possibly-missing-import = "error"
possibly-unresolved-reference = "error"
too-many-positional-arguments = "error"
unresolved-import = "error"
unresolved-reference = "error"
